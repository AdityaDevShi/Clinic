/**
 * Public, client-side identifiers (safe to ship in the app bundle — these are
 * the same values already exposed in the website's client code).
 */

// Firebase Web OAuth client (from google-services.json, client_type 3) — used
// to obtain a Firebase-compatible ID token from Google Sign-In.
export const WEB_CLIENT_ID =
    '650321118343-5c0qhsif9ntnn2282c3iudmf58t91inb.apps.googleusercontent.com';

// Razorpay public key id (same key the website checkout uses).
export const RAZORPAY_KEY_ID = 'rzp_live_SKlvmTJWZ1rbZr';
